const toMain = () => {
    window.location.replace("/");
}
const login = () => {
    window.location.replace("/auth/login");
}
const register = () => {
    window.location.replace("/auth/register");
}